@model List<string>

@{
    ViewData["Title"] = "Home Page";
}

<div class="p4">
    <h4 class="test-primary">Blob Storage Hierarchy</h4>

    @foreach (var item in Model)
    {
        <p>@item</p>
    }

</div>
